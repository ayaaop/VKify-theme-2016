{
  "name": "vkify16-themepack",
  "private": true,
  "description": "Build scripts for the VKify16 OpenVK themepack JS bundle.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
