{extends "@layout.xml"}

{block title}
    {_c_groups}
{/block}

{block heading}
    {_c_groups}
{/block}

{block content}
    <div class="page_padding">
        <form method="POST" style="margin-bottom: 20px;">
            <div class="settings_panel clear_fix">
                <div class="settings_list_row">
                    <div class="settings_label">{_admin_title}</div>
                    <div class="settings_labeled_text">
                        <input class="text" type="text" id="name" name="name" />
                        <input type="hidden" name="hash" value="{$csrfToken}" />
                        <input class="button" type="submit" value="{_add}">
                    </div>
                </div>
            </div>
        </form>

        <div class="page_table_wrapper">
            <table class="page_table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>{_admin_title}</th>
                        <th>{_admin_actions}</th>
                    </tr>
                </thead>
                <tbody>
                    <tr n:foreach="$groups as $group">
                        <td>
                            <a href="/admin/chandler/groups/{$group->id}">{$group->id}</a>
                        </td>
                        <td>
                            <span class="ui_tab_count" style="background-color: #3498db; color: white;">
                                {$group->name}
                            </span>
                        </td>
                        <td>
                            <a class="button" href="/admin/chandler/groups/{$group->id}">
                                {_edit}
                            </a>
                            <a class="button" href="/admin/chandler/groups/{$group->id}?act=permissions">
                                {_c_permissions}
                            </a>
                            <a class="button" href="/admin/chandler/groups/{$group->id}?act=members">
                                {_members}
                            </a>
                            <a class="button button_gray" href="/admin/chandler/groups/{$group->id}?act=delete">
                                {_delete}
                            </a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{/block}
