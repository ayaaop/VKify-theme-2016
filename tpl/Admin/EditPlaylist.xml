{extends "@layout.xml"}

{block title}
    {_edit} {$playlist->getName()}
{/block}

{block heading}
    {$playlist->getName()}
{/block}

{block content}
    <form method="POST">
        <div class="settings_panel clear_fix group_settings settings_padding">
            <div class="settings_list_row">
                <div class="settings_label">ID</div>
                <div class="settings_labeled_text" dir="auto">{$playlist->getId()}</div>
            </div>
            <div class="settings_list_row">
                <div class="settings_label">{_name}</div>
                <div class="settings_labeled_text">
                    <input class="text medium-field" type="text" id="name" name="name" value="{$playlist->getName()}" />
                </div>
            </div>
            <div class="settings_list_row">
                <div class="settings_label">{_description}</div>
                <div class="settings_labeled_text">
                    <textarea class="text medium-field" id="description" name="description" style="resize: vertical;">{$playlist->getDescription()}</textarea>
                </div>
            </div>
            <div class="settings_list_row">
                <div class="settings_label">{_admin_cover_id}</div>
                <div class="settings_labeled_text">
                    <div class="avatar_block">
                        <div class="avatar_block_inner">
                            <img src="{$playlist->getCoverUrl()}" style="object-fit: cover; width: 100px; height: 100px;" />
                        </div>
                    </div>
                    <br />
                    <input class="text medium-field" type="number" id="photo" name="photo" value="{$playlist->getCoverPhotoId()}" />
                </div>
            </div>
            <div class="settings_line"></div>
            <div class="settings_list_row">
                <div class="settings_label">{_owner}</div>
                <div class="settings_labeled_text">
                    <input class="text medium-field" type="number" id="owner_id" name="owner" value="{$playlist->getOwner()->getId()}" />
                </div>
            </div>
            <div class="settings_list_row">
                <div class="settings_label">{_deleted}</div>
                <div class="settings_labeled_text">
                    <label><input class="toggle-large" type="checkbox" id="deleted" name="deleted" value="1" {if $playlist->isDeleted()} checked {/if} /> {_deleted}</label>
                </div>
            </div>
            <div class="settings_line"></div>
            <div class="settings_list_row">
                <div class="settings_label"></div>
                <div class="settings_labeled_text">
                    <input type="hidden" name="hash" value="{$csrfToken}" />
                    <input class="button" type="submit" value="{_save}">
                </div>
            </div>
        </div>
    </form>
{/block}
